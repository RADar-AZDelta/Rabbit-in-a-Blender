SELECT DISTINCT * 
FROM (
{%- for column in events.values() -%}
    {%- if not loop.first %}
    UNION ALL
    {%- endif %}
    SELECT DISTINCT {{column}} as event_table
    FROM {{project_id}}.{{dataset_id_omop}}.{{omop_table}} 
{%- endfor %} 
)